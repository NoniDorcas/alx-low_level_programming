#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

/**
 *prototype functions to be used in
 * 0x14-bit_manipulation directory tasks.
 */

unsigned int binary_to_uint(const char *b);
void print_binary(unsigned long int n);

#endif
  
